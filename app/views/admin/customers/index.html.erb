<%= render "layouts/section", title: "会員一覧" %>
<table class="table">
  <thead class="thead-light">
    <tr>
      <th>会員ID</th>
      <th>氏名</th>
      <th>メールアドレス</th>
      <th>ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @customers.each do |c| %>
    <tr>
      <td><%= c.id %></td>
      <td><u><%= link_to c.full_name, admin_customer_path(c), class: "text-dark" %></u></td>
      <td><%= c.email %></td>
      <td><%= render "status", customer: c %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @customers %>
