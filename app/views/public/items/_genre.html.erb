<table>
  <thead>
    <tr><th> ジャンル検索 </th></tr>
    </thead>
  <tbody>
    <% Genre.all.each do |g| %>
    <tr>
      <td><%= link_to g.name, items_genre_path(g) %></td>
    </tr>
    <% end %>
  </tbody>
</table>